Tests to add:
- DAG parse test (no import errors).
- Task dependency test: extract >> transform >> load.
- Unit tests for mock_api.fetch_places and transform.compute_average_rating.
- Optional: integration test against sqlite to verify Daily_Metrics upsert.
